/* RGO OS frontend runtime config (served by the compose frontend container).
   In the local stack the API is reachable at localhost:8787. */
window.RGO_CONFIG = { apiBase: "http://localhost:8787" };
