/* RGO OS frontend runtime config. Copy to config.js and set your deployed API origin.
   This is the PRODUCTION configuration mechanism (not localStorage). */
window.RGO_CONFIG = { apiBase: "" /* e.g. "https://api.rgo.example" */ };
